- [x] `server/index.ts` 与 `server/app.ts` 已被正式收口为新服务端运行时入口，而不再只是最小脚手架
- [x] 新服务端运行时已具备可解释的基础中间件链
- [x] `/api/health` 已由 `Hono` 正式承接，并可返回最小健康状态
- [x] 新服务端已具备最小错误处理骨架，包括 `notFound` 与 `onError`
- [x] `server/lib/env.ts` 已统一主机、端口与 dist 目录等最小环境变量读取口径
- [x] 静态资源与前端壳托管预留位已明确，且能区分开发态与构建态差异
- [x] 本次子任务未越界到正式业务 API 迁移、完整认证 API 迁移或部署主线切换
- [x] 后续 API/认证阶段已不再需要继续依附旧 `src/app/api/*` 才能启动新服务端
