- [x] `Bill.type` 继续保持正式账单分类主口径，未被拆成新的正式类型
- [x] `OTHER` 账单新增了结构化条目名字段，且其职责与 `remarks` 清晰分离
- [x] 合同自动生成的钥匙押金账单会写入默认条目名
- [x] 合同自动生成的卫生费账单会写入默认条目名，展示名不再继续使用“清洁费”
- [x] 新增账单页仅在 `type='OTHER'` 时显示条目名输入入口
- [x] 编辑账单页仅在 `type='OTHER'` 时显示条目名编辑入口
- [x] 账单列表页桌面卡片不再统一使用 `￥` 作为所有账单的占位图标
- [x] 账单列表页与账单详情页中的 `OTHER` 账单能稳定展示条目名
- [x] 合同账单预览与 `fix_010` 应催缴汇总页复用了同一条目名与图标口径
- [x] 历史自动生成且可安全识别的 `OTHER` 账单可被回填条目名
- [x] 无法可靠识别条目名的历史 `OTHER` 账单保留了降级兼容，而不是被错误批量改写
- [x] `OTHER` 条目名未影响账单统计、筛选、状态语义和结算逻辑
- [x] `npm run lint` 通过
- [x] `npm run type-check` 通过
