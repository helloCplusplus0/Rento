- [x] 已建立统一的合同支付周期标准化真相源，并兼容中文值与历史英文值
- [x] 合同创建页账单预展示与服务端实际落库账单使用同一套周期与 RENT 账单计算逻辑
- [x] 合同创建、续租、手工重新生成账单与缺失 RENT 账单补齐路径已统一复用共享逻辑
- [x] `12` 个月 `半年付` 合同会生成 `2` 条 RENT 账单，且每条金额等于 `monthlyRent * 6`
- [x] `季付` 或 `年付` 的 RENT 账单条数与金额正确
- [x] 押金、钥匙押金、清洁费账单未被误伤
- [x] 已定义历史错误 RENT 账单识别规则
- [x] 自动历史修复已严格限制在“全部 `PENDING` 且 `receivedAmount = 0`”的安全范围
- [x] 已支付、部分支付、已完成或人工修改账单不会被自动批量改写
- [x] `npm run lint` 通过
- [x] `npm run type-check` 通过
