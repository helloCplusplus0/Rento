- [x] 已冻结合同状态事实与状态跟踪提示的语义边界
- [x] 续租完成后的旧合同不再保留为 `EXPIRED`
- [x] 退租完成后的旧合同继续保持 `TERMINATED`
- [x] 合同列表卡片中的状态跟踪提示已移动到状态指示左侧
- [x] 合同详情页 Hero 已使用与列表页一致的状态跟踪提示语义
- [x] 列表页、详情页与提醒组件已统一使用 `contractExpiryAlertDays` 口径
- [x] `expiredCount`、到期提醒与 `expiring_soon` 不再把已续租承接旧合同计入待处理到期合同
- [x] 已明确历史 `isExtended=true && status='EXPIRED'` 合同的最小盘点/修复边界
- [x] `npm run lint` 通过
- [x] `npm run type-check` 通过
- [x] 已完成合同列表页与合同详情页的人工复核：`ACTIVE`、续租后旧合同、退租后旧合同样本逐项确认；本地验收库当前无残留真实 `EXPIRED` 样本，并已通过 `已到期` 筛选空态确认不存在误计入结果
- [x] 合同列表页已按“距到期日最近优先 + `TERMINATED` 后置”排序，浏览器验收中 `CT202606591182` 位于 `CT202606996967` 之前
- [x] 房间列表页 `/rooms` 已不再信任脏的 `Room.status` 快照，`A101` 已与活跃合同 `CT202606591182` 同步显示为“在租 / 计家巧”
- [x] 房间详情页 `/rooms/:id` 已与房间列表/活跃合同同步，`A101` 详情显示“当前状态=在租中 / 当前租客=计家巧 / 合同编号=CT202606591182”
- [x] `npm run fix:017:room-occupancy-repair` dry-run 为 `0`，本地最小修复后无残留房态快照不一致样本
