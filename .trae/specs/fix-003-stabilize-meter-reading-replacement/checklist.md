- [x] `MeterReading` 已具备结构化 `recordType`，并覆盖合同初始底数、正常抄表、退租最终抄表三类路径
- [x] 抄表重复校验已基于提交的 `readingDate` 与 `meterId` 精确判断，不再使用服务器当前日期替代
- [x] 换表路径下的新表正式抄表不会被误判为“今日已存在该仪表抄表记录”
- [x] 同一次有效抄表提交生成的聚合水电账单不会遗漏本应纳入的电表或水表项目
- [x] 合同创建当天存在初始底数时，同日正式抄表不会因底数记录被误判重复
- [x] 抄表历史页已改为基于结构化 `recordType` 展示和筛选，不再依赖备注关键字猜测
- [x] 抄表历史页删除入口与服务端、查询层删除策略保持一致，不再存在伪能力
- [x] 房间详情页“移除仪表”文案已准确反映当前真实行为，不再把停用保留历史误写为解绑
- [x] 已通过 `npm run lint`
- [x] 已通过 `npm run type-check`
- [x] 已补并执行至少一条“停用旧表 + 新增新表 + 再次抄表 + 聚合账单完整”的主链验证
- [x] 已补并执行至少一条“合同创建当天底数 + 同日正式抄表”的验证路径
- [x] 主页快捷操作 -> `批量抄表` 入口可达，目标页面可正常打开，且页面已适配 fix_003 的提交与回执结构
