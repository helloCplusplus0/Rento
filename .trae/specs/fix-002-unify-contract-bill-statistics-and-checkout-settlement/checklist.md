- [x] 已建立统一的账单状态展示统计工具，并兼容合同详情页、账单列表页与共享统计模块
- [x] 合同详情页“已逾期 / 待处理 / 已结清”统计已不再使用页面本地手写条件
- [x] 账单列表页与共享账单统计模块对 `PAID / COMPLETED / OVERDUE / PENDING` 的展示与聚合结果一致
- [x] 新创建但未逾期的账单不会在合同详情页被误显示为“已逾期”
- [x] 已建立统一的退租结算计算器，前端预览与后端落库复用同一套逻辑
- [x] 退租页面编辑后的结算结果会作为正式载荷提交后端，并真正参与校验与落库
- [x] 应退项目允许在 `0 ~ originalAmount` 安全边界内调整本次退租结算退款金额
- [x] 退租确认后始终会生成一张正式退租结算账单；当最终净额为 `0` 时也会生成金额为 `0` 的账单
- [x] 新生成的退租结算账单会直接进入 `COMPLETED` 闭环态
- [x] 已完全结清账单在退租结算中不可编辑
- [x] 未完全结清账单仅允许在 `0 ~ pendingAmount` 安全边界内调整本次结算金额
- [x] 退租确认后，关联旧未付账单都会被统一收口为闭环态；若存在未全额纳入部分，会保留退租减免审计说明
- [x] 部分收款账单的退租编辑不会破坏既有已收事实
- [x] 退租确认后会保留调整原因、操作人、原始金额和调整金额等审计线索
- [x] 已定义历史退租结算账单的不一致识别规则
- [x] 自动历史修复评估已严格限制在安全范围内，超出边界的记录只输出审计清单
- [x] `npm run lint` 通过
- [x] `npm run type-check` 通过
