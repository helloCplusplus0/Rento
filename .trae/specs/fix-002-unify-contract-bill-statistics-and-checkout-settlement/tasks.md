# Tasks
- [x] Task 1: 冻结统一账单状态展示统计工具：抽离合同详情页、账单列表页与共享统计模块复用的状态展示统计逻辑。
  - [x] SubTask 1.1: 明确“已逾期 / 待处理 / 已结清”的共享展示口径，并落为共享工具
  - [x] SubTask 1.2: 将合同详情页改为复用共享统计工具，移除本地手写状态判断
  - [x] SubTask 1.3: 将账单列表页与共享统计模块改为复用统一口径，收口 `PAID / COMPLETED / OVERDUE / PENDING` 的展示差异

- [x] Task 2: 冻结统一退租结算计算器：让退租页面预览与后端落库复用同一套结算逻辑。
  - [x] SubTask 2.1: 抽离共享退租结算计算器，统一应退、应收、旧账单处理与结算汇总口径
  - [x] SubTask 2.2: 让退租页面预览改为复用共享计算器，不再本地维护第二套结算公式
  - [x] SubTask 2.3: 让退租 API 改为复用同一共享计算器，保证预览与落库一致

- [x] Task 3: 落地退租结算受限人工调整：把前端编辑结果变为正式业务输入，并在后端建立安全校验边界。
  - [x] SubTask 3.1: 设计正式结算明细载荷，包含原始金额、调整金额、调整原因和结算项来源
  - [x] SubTask 3.2: 冻结应退项目与旧未付账单的受限编辑边界，分别限制在 `0 ~ originalAmount` 与 `0 ~ pendingAmount`
  - [x] SubTask 3.3: 在后端校验部分收款账单的调整不会破坏既有已收事实，并保留审计线索
  - [x] SubTask 3.4: 退租确认后始终生成正式退租结算账单（含 `0` 金额平结场景），并将新旧关联账单统一收口为闭环态

- [x] Task 4: 设计历史退租结算账单审计边界：识别历史预览与落库可能不一致的退租结算账单，并冻结自动修复安全范围。
  - [x] SubTask 4.1: 定义历史退租结算账单识别规则与审计输出结构
  - [x] SubTask 4.2: 明确仅在 `PENDING`、`receivedAmount = 0`、无人工修改痕迹等安全边界内才允许进一步自动修复评估
  - [x] SubTask 4.3: 对已支付、已完成、已人工修改或无法准确回溯意图的记录只输出审计清单，不自动改写

- [x] Task 5: 完成主链验证与回归校验：验证统计展示、退租结算和历史审计边界符合分析文档要求。
  - [x] SubTask 5.1: 验证合同详情页存在新建未付账单时，“已逾期”统计为 `0`
  - [x] SubTask 5.2: 验证账单列表页与共享统计模块的状态聚合结果一致
  - [x] SubTask 5.3: 验证退租页面预览与最终创建的退租结算账单金额完全一致
  - [x] SubTask 5.4: 验证已完全结清账单不可编辑、未完全结清账单仅在安全边界内可编辑
  - [x] SubTask 5.5: 运行 `npm run lint` 与 `npm run type-check`

# Task Dependencies
- `Task 2` depends on `Task 1`
- `Task 3` depends on `Task 2`
- `Task 4` depends on `Task 3`
- `Task 5` depends on `Task 1`, `Task 2`, `Task 3`, and `Task 4`
