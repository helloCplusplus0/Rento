- [x] 已明确 `phase04-02` 的高优先级性能热点与收口范围
- [x] `/api/bills` 不再依赖明显的全量查询后内存过滤与分页
- [x] `/api/contracts` 不再依赖明显的全量查询后内存搜索
- [x] 房间与租客列表的旧查询路径已按最小范围完成数据库侧收口或明确治理结论
- [x] 必要的统计查询未再沿用明显的全量读数后内存聚合路径
- [x] 至少一条性能或查询行为验证路径可执行
- [x] `npm run lint` 与 `npm run type-check` 通过
- [x] 当前子任务未引入新的缓存系统、外部性能平台或主数据模型重写
