- [x] 已在 `src/minix/` 中建立统一布局主线，承接旧 `AppLayout` 的核心壳结构
- [x] 已在 `src/minix/` 中迁移或适配 `UnifiedNavigation`，并改由 `React Router` 驱动导航状态
- [x] `/`、`/rooms`、`/add`、`/contracts`、`/bills`、`/settings` 已具备正式页面承接位
- [x] `login`、`offline`、`404`、`error`、`loading` 已具备基础承接位
- [x] 现有 UI 样式、图标语义与信息架构已被延续，未被重做为另一套设计系统
- [x] 新前端壳已明确复用现有路由元数据、页面治理口径或其等价承接方式
- [x] 本次子任务未越界到全量业务页面逻辑迁移、完整查询写操作迁移或完整认证链迁移
- [x] 新前端壳已具备统一前端宿主能力，不再只是 `phase07-01` 的最小占位壳
