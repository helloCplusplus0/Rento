- [x] `phase16-04` 的 spec 明确要求在云端证据未闭环前，把当前轮结论标记为“未通过但单值化”
- [x] spec 明确要求 legacy 资产保持 rollback-only 身份，不得误写为当前正式入口
- [x] spec 明确要求冻结 legacy 资产退出顺序、保留条件、回滚窗口与后续归档前提
- [x] spec 明确要求本轮不删除 legacy 资产、不伪造正式人工 HTTPS 验收、正式部署演练或 legacy 回滚演练结果
- [x] spec 明确要求 `docs/phase16_*`、`DEPLOYMENT.md` 与根级真相源保持单一解释
- [x] spec 明确要求后续只有在真实云服务器补齐证据后，才允许重新判断 `phase16` 是否“通过”
- [x] tasks 将 legacy 资产盘点、最终结论冻结、root sync 与文档验收拆成可验证的顺序任务
- [x] `phase16-04` 的产出能够作为后续真实云服务器验收完成后的最终复判入口
